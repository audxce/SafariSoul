@page
@model SafariSoul.Pages.DataForms.EducationProgramCRUD.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_NewLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EducationProgram[0].ProgramName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EducationProgram[0].ProgramDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EducationProgram) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProgramName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProgramDescription)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.EducationProgramId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.EducationProgramId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.EducationProgramId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
